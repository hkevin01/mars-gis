# MARS-GIS Docker Configuration - REDIRECT
# 
# ⚠️ IMPORTANT: Docker files have been moved to docker/ directory
# 
# NEW STRUCTURE:
#   docker/
#   ├── backend/Dockerfile
#   ├── frontend/Dockerfile  
#   ├── compose/
#   │   ├── docker-compose.yml           # Main development environment
#   │   ├── docker-compose.prod.yml      # Production environment
#   │   ├── docker-compose.test.yml      # Testing environment
#   │   └── docker-compose.override.yml  # Development overrides
#   ├── docker-helper.sh                 # Management script
#   └── README.md                        # Complete documentation
#
# USAGE:
#   
#   # Use the helper script (RECOMMENDED):
#   ./docker/docker-helper.sh dev        # Start development environment
#   ./docker/docker-helper.sh prod       # Start production environment  
#   ./docker/docker-helper.sh test       # Run tests
#   ./docker/docker-helper.sh help       # See all available commands
#
#   # Or use docker-compose directly:
#   cd docker/compose
#   docker-compose up -d                 # Development environment
#   docker-compose --profile production up -d  # Production
#   docker-compose -f docker-compose.test.yml up  # Testing
#
# See docker/README.md for complete documentation

version: "3.8"

services:
  # Migration notice service
  migration-notice:
    image: alpine:latest
    container_name: mars-gis-migration-notice
    command: >
      sh -c "
      echo '';
      echo '🚀 MARS-GIS Docker Migration Notice';
      echo '';
      echo '⚠️  Docker files have been moved to docker/ directory';
      echo '';
      echo '📋 Please use one of these commands:';
      echo '   ./docker/docker-helper.sh dev    # Development environment';
      echo '   ./docker/docker-helper.sh prod   # Production environment';
      echo '   ./docker/docker-helper.sh test   # Run tests';
      echo '   ./docker/docker-helper.sh help   # See all commands';
      echo '';
      echo '📚 See docker/README.md for complete documentation';
      echo '';
      sleep 5;
      exit 0;
      "

networks:
  mars-network:
    driver: bridge
