# MARS-GIS Override Configuration - REDIRECT
# 
# ⚠️ IMPORTANT: Docker files have been moved to docker/ directory
# 
# NEW LOCATION: docker/compose/docker-compose.override.yml
#
# USAGE:
#   cd docker/compose && docker-compose up -d
#   # (override file is automatically loaded)
#
# See docker/README.md for complete documentation

version: "3.8"

services:
  migration-notice-override:
    image: alpine:latest
    command: >
      sh -c "
      echo '⚙️ MARS-GIS Override Configuration';
      echo '⚠️  Moved to: docker/compose/docker-compose.override.yml';
      echo '📋 Use: cd docker/compose && docker-compose up -d';
      sleep 3; exit 0;
      "
