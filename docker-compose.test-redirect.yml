# MARS-GIS Testing Configuration - REDIRECT
#
# ⚠️ IMPORTANT: Docker files have been moved to docker/ directory
#
# NEW LOCATION: docker/compose/docker-compose.test.yml
#
# USAGE:
#   ./docker/docker-helper.sh test
#   # OR
#   cd docker/compose && docker-compose -f docker-compose.test.yml up
#
# See docker/README.md for complete documentation

version: "3.8"

services:
  migration-notice-test:
    image: alpine:latest
    command: >
      sh -c "
      echo '🧪 MARS-GIS Testing Configuration';
      echo '⚠️  Moved to: docker/compose/docker-compose.test.yml';
      echo '📋 Use: ./docker/docker-helper.sh test';
      sleep 3; exit 0;
      "
