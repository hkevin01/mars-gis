# MARS-GIS Production Configuration - REDIRECT
#
# ⚠️ IMPORTANT: Docker files have been moved to docker/ directory
#
# NEW LOCATION: docker/compose/docker-compose.prod.yml
#
# USAGE:
#   ./docker/docker-helper.sh prod
#   # OR
#   cd docker/compose && docker-compose --profile production up -d
#
# See docker/README.md for complete documentation

version: "3.8"

services:
  migration-notice-prod:
    image: alpine:latest
    command: >
      sh -c "
      echo '🚀 MARS-GIS Production Configuration';
      echo '⚠️  Moved to: docker/compose/docker-compose.prod.yml';
      echo '📋 Use: ./docker/docker-helper.sh prod';
      sleep 3; exit 0;
      "
